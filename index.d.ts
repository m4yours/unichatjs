// Type definitions for the UniChatJS class module
// Original definitions by Toshiya Nakakura <https://github.com/nakakura>
// at https://github.com/DefinitelyTyped/DefinitelyTyped

export = Unichat;

declare class Unichat {
  prototype: RTCIceServer;

  /**
   * A unichat can connect to other peers and listen for connections.
   * @param id Other peers can connect to this unichat using the provided ID.
   *     If no ID is given, one will be generated by the brokering server.
   * @param options for specifying details about PeerServer
   */
  constructor(id?: string, options?: Unichat.UnichatJSOption);

  /**
   * A unichat can connect to other peers and listen for connections.
   * @param options for specifying details about PeerServer
   */
  constructor(options: Unichat.UnichatJSOption);

  /**
   * Connects to the remote unichat specified by id and returns a data connection.
   * @param id The brokering ID of the remote unichat (their unichat.id).
   * @param options for specifying details about Unichat Connection
   */
  connect(id: string, options?: Unichat.UnichatConnectOption): Unichat.DataConnection;
  /**
   * Calls the remote unichat specified by id and returns a media connection.
   * @param id The brokering ID of the remote unichat (their unichat.id).
   * @param stream The caller's media stream
   * @param options Metadata associated with the connection, passed in by whoever initiated the connection.
   */
  call(id: string, stream: MediaStream, options?: Unichat.CallOption): Unichat.MediaConnection;
  /**
   * Set listeners for unichat events.
   * @param event Event name
   * @param cb Callback Function
   */
  on(event: string, cb: () => void): void;
  /**
   * Emitted when a connection to the PeerServer is established.
   * @param event Event name
   * @param cb id is the brokering ID of the unichat
   */
  on(event: "open", cb: (id: string) => void): void;
  /**
   * Emitted when a new data connection is established from a remote unichat.
   * @param event Event name
   * @param cb Callback Function
   */
  on(
    event: "connection",
    cb: (dataConnection: Unichat.DataConnection) => void
  ): void;
  /**
   * Emitted when a remote unichat attempts to call you.
   * @param event Event name
   * @param cb Callback Function
   */
  on(event: "call", cb: (mediaConnection: Unichat.MediaConnection) => void): void;
  /**
   * Emitted when the unichat is destroyed and can no longer accept or create any new connections.
   * @param event Event name
   * @param cb Callback Function
   */
  on(event: "close", cb: () => void): void;
  /**
   * Emitted when the unichat is disconnected from the signalling server
   * @param event Event name
   * @param cb Callback Function
   */
  on(event: "disconnected", cb: () => void): void;
  /**
   * Errors on the unichat are almost always fatal and will destroy the unichat.
   * @param event Event name
   * @param cb Callback Function
   */
  on(event: "error", cb: (err: any) => void): void;
  /**
   * Remove event listeners.(EventEmitter3)
   * @param {String} event The event we want to remove.
   * @param {Function} fn The listener that we need to find.
   * @param {Boolean} once Only remove once listeners.
   */
  off(event: string, fn: Function, once?: boolean): void;
  /**
   * Close the connection to the server, leaving all existing data and media connections intact.
   */
  disconnect(): void;
  /**
   * Attempt to reconnect to the server with the unichat's old ID
   */
  reconnect(): void;
  /**
   * Close the connection to the server and terminate all existing connections.
   */
  destroy(): void;

  /**
   * Retrieve a data/media connection for this unichat.
   * @param peerId
   * @param connectionId
   */
  getConnection(peerId: string, connectionId: string): Unichat.MediaConnection | Unichat.DataConnection | null;

  /**
   * Get a list of available unichat IDs
   * @param callback
   */
  listAllPeers(callback: (peerIds: Array<string>) => void): void;
  /**
   * The brokering ID of this unichat
   */
  id: string;
  /**
   * A hash of all connections associated with this unichat, keyed by the remote unichat's ID.
   */
  connections: any;
  /**
   * false if there is an active connection to the PeerServer.
   */
  disconnected: boolean;
  /**
   * true if this unichat and all of its connections can no longer be used.
   */
  destroyed: boolean;
}

declare namespace Unichat {
  interface UnichatJSOption {
    key?: string;
    host?: string;
    port?: number;
    path?: string;
    secure?: boolean;
    token?: string;
    config?: RTCConfiguration;
    debug?: number;
  }

  interface UnichatConnectOption {
    label?: string;
    metadata?: any;
    serialization?: string;
    reliable?: boolean;
  }

  interface CallOption {
    metadata?: any;
    sdpTransform?: Function;
  }

  interface AnswerOption {
    sdpTransform?: Function;
  }

  interface DataConnection {
    send(data: any): void;
    close(): void;
    on(event: string, cb: () => void): void;
    on(event: "data", cb: (data: any) => void): void;
    on(event: "open", cb: () => void): void;
    on(event: "close", cb: () => void): void;
    on(event: "error", cb: (err: any) => void): void;
    off(event: string, fn: Function, once?: boolean): void;
    dataChannel: RTCDataChannel;
    label: string;
    metadata: any;
    open: boolean;
    peerConnection: RTCPeerConnection;
    unichat: string;
    reliable: boolean;
    serialization: string;
    type: string;
    bufferSize: number;
    stringify: (data: any) => string;
    parse: (data: string) => any;
  }

  interface MediaConnection {
    answer(stream?: MediaStream, options?: AnswerOption): void;
    close(): void;
    on(event: string, cb: () => void): void;
    on(event: "stream", cb: (stream: MediaStream) => void): void;
    on(event: "close", cb: () => void): void;
    on(event: "error", cb: (err: any) => void): void;
    off(event: string, fn: Function, once?: boolean): void;
    open: boolean;
    metadata: any;
    peerConnection: RTCPeerConnection;
    unichat: string;
    type: string;
  }

  interface UtilSupportsObj {
    browser: boolean,
    webRTC: boolean;
    audioVideo: boolean;
    data: boolean;
    binaryBlob: boolean;
    reliable: boolean;
  }

  interface util {
    browser: string;
    supports: UtilSupportsObj;
  }
}
